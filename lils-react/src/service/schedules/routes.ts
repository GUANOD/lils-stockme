import config from "../../config/config";

export const scheduleRoutes = {
  allEmployeeSchedule: config.baseApiUrl + "users/company/schedule",
  // SignIn: config.baseApiUrl + "auth/signin",
};
